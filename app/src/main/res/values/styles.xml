<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.AppTheme">
        <!-- Customize your theme here. -->

    </style>

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/background</item>
    </style>

    <style name="TextAppearence.App.TextInputLayoutLight" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/textLight</item>
        <item name="colorAccent">@android:color/holo_purple</item>

    </style>

    <style name="TextAppearence.App.Prueba" parent="@android:style/TextAppearance">
        <item name="colorAccent">@android:color/holo_purple</item>
        <item name="android:textColorHint">@android:color/holo_green_dark</item>
        <item name="android:textColor">@color/textDark</item>
    </style>

    <style name="TextAppearence.App.TextInputLayoutDark" parent="@android:style/TextAppearance">
        <item name="colorAccent">@android:color/holo_purple</item>
        <item name="android:textColorHint">@android:color/holo_purple</item>
        <item name="android:textColor">@color/textDark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />∫


</resources>
